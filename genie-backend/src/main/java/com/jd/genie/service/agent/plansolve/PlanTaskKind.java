package com.jd.genie.service.agent.plansolve;

/** The executable node types supported by the Plan-Solve DAG. */
public enum PlanTaskKind {
    OCR,
    IMAGE_GENERATE,
    IMAGE_EDIT,
    HUMAN_CONFIRMATION
}
